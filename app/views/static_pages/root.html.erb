<% if signed_in? %>
  <script>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  </script>
<% end %>

<div id='app-display'>
  Check Chrome console (alt + cmd + j)...
  Check Webpack tab.
  Check Rails Server tab
</div>
